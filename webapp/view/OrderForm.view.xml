<mvc:View
    controllerName="orders.controller.OrderForm"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns="sap.m"
    xmlns:f="sap.ui.layout.form"
    xmlns:core="sap.ui.core"
    displayBlock="true">

    <Page id="orderFormPage" title="{viewModel>/viewTitle}" showNavButton="true" navButtonPress=".onNavBack">
        <content>
            <VBox class="sapUiResponsiveMargin">
                
                <f:SimpleForm 
                    id="orderForm"
                    class="myCustomCard"
                    editable="true"
                    layout="ColumnLayout"
                    title="{i18n>mainDataTitle}" 
                    columnsM="2" columnsL="2" columnsXL="2">
                    <f:content>
                        <Label text="{i18n>colOrderID}" />
                        <Input value="{formModel>/NumOrdine}" editable="false" />
                        <Label text="{i18n>colCustomer}" required="true"/>
                        <Input value="{formModel>/Cliente}" placeholder="{i18n>placeholderCustomer}" />

                        <Label text="{i18n>colStatus}" />
                        <Select selectedKey="{formModel>/Stato}">
                            <core:Item key="1" text="{i18n>statusCreated}" /> 
                            <core:Item key="2" text="{i18n>statusProcessing}" />
                            <core:Item key="3" text="{i18n>statusTransit}" />
                            <core:Item key="4" text="{i18n>statusClosed}" />
                        </Select>

                        <Label text="{i18n>colTotalAmount}" />
                        <Input value="{formModel>/ImportoTot}" type="Number" description="€" editable="false" />
                    </f:content>
                </f:SimpleForm>

            </VBox>

            <Table id="orderItemsTable"
                   items="{formModel>/Articoli}" 
                   class="myCustomCard sapUiResponsiveMargin"
                   width="auto">
                <headerToolbar>
                    <Toolbar>
                        <Title text="{i18n>orderItemsTitle}" level="H2"/>
                        <ToolbarSpacer />
                        <Button icon="sap-icon://add" text="{i18n>btnNewArticle}" type="Emphasized" press=".onAddArticleToOrder" />
                    </Toolbar>
                </headerToolbar>
                <columns>
                    <Column width="20%"><Text text="{i18n>colArticleCode}"/></Column>
                    <Column width="40%"><Text text="{i18n>colArticleName}"/></Column>
                    
                    <Column width="20%" hAlign="End"><Text text="{i18n>colQuantity}"/></Column>
                    <Column width="20%" hAlign="End"><Text text="{i18n>colPrice}"/></Column>
                </columns>
                <items>
                    <ColumnListItem>
                        <cells>
                            <Input value="{formModel>CodArticolo}" />
                            <Input value="{formModel>NomeArticolo}" />
                            
                            <Input value="{formModel>QuantitaOrdine}" type="Number" textAlign="End" />
                            <Input value="{formModel>Importo}" type="Number" textAlign="End" />
                        </cells>
                    </ColumnListItem>
                </items>
            </Table>
        </content>

        <footer>
            <OverflowToolbar>
                <ToolbarSpacer/>
                <Button text="{i18n>btnSave}" type="Emphasized" icon="sap-icon://save" press=".onSave" />
                <Button text="{i18n>btnCancel}" type="Transparent" icon="sap-icon://sys-cancel" press=".onCancel" />
            </OverflowToolbar>
        </footer>
    </Page>
</mvc:View>