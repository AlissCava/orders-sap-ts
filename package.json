{
    "name": "orders.orders",
    "scripts": {
        "build": "mbt build --mtar orders.orders.mtar",
        "build:workspaces": "npm run build --workspaces --if-present",
        "deploy": "cf deploy mta_archives/orders.orders.mtar --retries 0",
        "lint": "npm run lint --workspaces --if-present",
        "ui5lint": "npm run ui5lint --workspaces --if-present",
        "start:webapp": "npm start --workspace webapp"
    },
    "dependencies": {
        "mbt": "^1"
    },
    "workspaces": [
        "webapp"
    ]
}